<div class="row">
<div class="col-sm-12">
<div class="panel">
<div class="panel-body">
<h1 class="panel-title">{{title}}</h1>
<div class="row">
  <div class="col-sm-12">
    <p>
      These products are available from external vendors to replace 
      existing inventory items.  Available information includes retail price,
      the number of stores in the market that carry the product, and the 
      average sales in the market.
    </p>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>Type</th>
          <th>Retail Price</th>
          <th>Market Average</th>
          <th>Number of Stores</th>
        </tr>
      </thead>
      <tbody>
        {{#each products}}
        <tr>
          <td><abbr title="{{description}}">{{name}}</abbr></td>
          <td>{{type}}</td>
          <td>{{retailPrice}}</td>
          <td>{{marketAverage}}</td>
          <td>{{numStores}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
</div>
</div>
</div>
</div>